# vCenter Security Audit Tool (vSAT) Dependencies

# Required
pyvmomi>=8.0.0.1
requests>=2.28.0

# Optional - for Windows integrated authentication (SSPI/NTLM)
# Install on Windows: pip install pywin32 requests-ntlm
# pywin32>=306
# requests-ntlm>=1.2.0

# Optional - for Kerberos authentication (Linux/Mac/Windows)
# Install: pip install requests-kerberos
# requests-kerberos>=0.14.0

# Optional - for scheduled scans with email notifications
# pyyaml>=6.0
